# Copy to .env and fill in. Only this example file is ever committed (§9.2).

# Path to your PRIVATE vault — its own git repo, never tracked by the code repo
# (§9.1). Required with no default: a missing value fails loudly rather than
# silently writing memory somewhere git-tracked.
BRAIN_VAULT_PATH=./vault

# OpenAI — used by the consolidator from P2 on (§5.8)
OPENAI_API_KEY=sk-REPLACE_ME

# ── Gateway over HTTP (P5, §3.1/§4.3) — only needed where the compose stack
# runs (the VM's deploy/compose/.env), not for local CLI use.
# GATEWAY_ISSUER=https://REPLACE_ME.auth0.com/
# GATEWAY_AUDIENCE=brain-gateway
# GATEWAY_RESOURCE=https://REPLACE_ME.tailnet.ts.net/mcp
# BRAIN_DATA_DIR=/data

# ── Auth0 tenant configuration (one-time, scripts/auth0-setup.ts — §13).
# A Management-API M2M credential; delete or rotate after setup if you like.
# AUTH0_DOMAIN=REPLACE_ME.us.auth0.com
# AUTH0_MGMT_CLIENT_ID=REPLACE_ME
# AUTH0_MGMT_CLIENT_SECRET=REPLACE_ME

# ── SessionEnd remote delivery (§6.4) — laptop only, printed by auth0-setup.
# BRAIN_GATEWAY_URL=https://REPLACE_ME.tailnet.ts.net/mcp
# BRAIN_HOOK_CLIENT_ID=REPLACE_ME
# BRAIN_HOOK_CLIENT_SECRET=REPLACE_ME
# BRAIN_HOOK_AUDIENCE=brain-gateway
